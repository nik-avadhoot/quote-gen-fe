# Open work register

This is the single startup register. Detailed packets remain linked as evidence, but they do not
automatically expand the current increment. Reclassify an item here when evidence or Product Owner
direction changes its status.

| Classification | Item | Current boundary / clearing action |
|---|---|---|
| **Speedbreaker** | S9 activation and closure | Do not claim or rely on production S9 mutations until the approved attestation secret is provisioned, the Edge Function is deployed, the authenticated Calculate/Send/workflow paths and Maker/Checker/Admin boundaries are exercised, persistent evidence is inspected, affected regressions pass, the real browser journey succeeds, and the Product Owner accepts it. This is a **narrow** stop against enabling or relying on production Quote mutations only; it does not block independent U1–U5 implementation, local verification, fixture-browser work, read-only surfaces, or deployment-ready preparation (restated 2026-09-15). Product Owner walkthroughs are **unavailable and deferred, not failed**. Every S9-dependent workflow action stays visibly disabled as `Backend activation pending`. See [`s9-technical-closure-and-u3-u6-handoff.md`](s9-technical-closure-and-u3-u6-handoff.md). |
| **Follow-up debt** | Family G authenticated-read correction (deployment-dependent) | Committed in `quote-gen-be` `ac67e39`, **not applied to any remote project**. `20260915180000_s9_1_fix_family_g_read_helper_execute.sql` grants EXECUTE on `app_private.can_read_quote_family/_revision/_item` to `authenticated` only (anon/public nothing, no service-role path, `app_private` stays unexposed) and inverts QG-38 to authenticated-can/anon-cannot rather than deleting it. Static contract `tests/test_family_g_read_helper_grant_contract.py` 20/0, including a simulated splice against the recorded QG-38 body. Until applied, caller-token Quote History/Approval Inbox reads that reach a Family G row return 403. Applying it needs production database authority; the seeding plan is in the untracked `s9-qualification-seed-and-quote-read-fix-plan.md`. |
| **Fixed, verified live** | GSM Master and U4 Family/Sector write correction | Found 2026-09-16 with read-only probes: the public invoker wrappers `add_paper_gsm_value`, `set_paper_gsm_value_status`, `propose_customer_family`, `create_minimal_prospect` and `add_customer_family_sector` called `app_private` SECURITY DEFINER functions that `authenticated` could not execute, so every signed-in call raised 42501 and the route returned `CAPABILITY_REQUIRED` (403) whatever the caller's capabilities. `quote-gen-be` `513b442` + `11f41ff`: migration `20260916165004_fix_gsm_and_u4_definer_execute_grants.sql` grants EXECUTE to `authenticated` on exactly those five signatures (public/anon stay revoked; the `family_b` convention), keeps the private-only compatibility overloads `propose_customer_family(text)` / `create_minimal_prospect(text, bigint)` closed, repoints GSM-6 to authenticated-can/anon-cannot rather than deleting it, and aborts unless every public invoker wrapper's private callee is executable by `authenticated`. Static contract `tests/test_definer_execute_grant_contract.py` 31/0. **Applied to the live project 2026-09-16 with owner approval** (authored as `20260916180000`, renamed to the recorded version). Live verification: as `authenticated`, all five wrappers now fail inside the body with their capability error instead of `permission denied for function`; `tests.gsm_master_catalogue()` 6/6 and `tests.u4_customer_family_sector_catalogue()` 7/7. Not yet exercised through the Flask routes or an authenticated browser session. Amendment 02/03/04 were subsequently applied under their recorded 2026-09-17 versions; the Family G read correction remains unapplied. |
| **Fixed, expanded by Amendment 04** | U2 SKU Master — catalogue, detail and governed editing | The caller-scoped catalogue/detail foundation landed 2026-09-15 and Amendment 04 slice 1 landed in frontend `603af76` and backend `863e652`/`c515108`/`4e4cbde`. The current page exposes named governed proposal, version, approval, code, lifecycle, portfolio and reference actions plus history; every write carries CAS and uses backend routes. Current gates: frontend 126/0, route 195/0, Amendment 04 static contract 85/0. All five SKU migrations are live; authenticated-live browser mutation proof and Product Owner validation remain deferred. |
| **Fixed, applied live** | SKU Master page — Canonical Amendment 02 | CDM-43/44 field scope and SKU Sets landed 2026-09-16. Migration `20260917024849_u2_sku_master_quote_fields_and_sets` is applied live (the live SKU tables were empty, so nothing was backfilled). The split catalogue/detail surface, 40-field registry after Amendment 03, SKU Set reads and production-data backlog remain fixture-browser verified only. |
| **Fix in current increment** | SKU Master — one search box over identity only | Landed 2026-09-16. `q` now matches ANY identity factor and nothing else: Plant Item Code, the SKU version's Item Name and Item Short Name, the Customer Item Code and SoftComp Code external references, and the owning Customer's name. Every word must match somewhere, so words narrow ("Distillers 375" finds a Customer plus a code fragment). Deliberately NOT searched: lifecycle, plant, portfolio and every specification field — those keep their existing dropdowns. Server-side and caller-scoped by construction: every sub-query carries the caller's token and `plant_id in <plant_access plants>`, and the Customer pass reads party identity only before re-entering that same plant-scoped query, so no other plant's or tenant's rows are ever loaded to be filtered in memory. Status and Customer/Family filters bound the search itself. The response's `search.fields` states which of the six were ACTUALLY searched — Item Name and Item Short Name report `schema_pending` until the Amendment 02 migration is applied, Customer name reports `not_visible_to_caller` without `read_party_master`, a failed read reports `unavailable` — and `search.scan_truncated` reports a per-field scan bound, so the box degrades visibly instead of missing rows in silence. Empty answers stay four distinct claims (no plant access / no identity match / no filter match / nothing visible). Stable error codes unchanged; a sixth word is `INVALID_INPUT` before any read. Backend `backend `f8ff21d``, frontend `frontend `ebaf2ad``. Gates: route 117/0, `test:sku-master` 87/0, eslint clean on changed files, module-contract build exit 0. Fixture-browser verified via the `u2-skus` developer preview only — **not** authenticated-live. |
| **Follow-up debt** | `parseConstrQuery` is an unwired conversational filter parser | Observed 2026-09-16 while widening the SKU Master search. `src/state/useBatchState.js:134` defines a free-text parser ("active alcobev ITC BS>8 GSM 700-750 Cobb 125") that sets Construction-overlay filter state; it is returned from the hook (line 218) but no component calls it. It was deliberately NOT wired into the SKU Master identity search: that search is one box over identity factors, server-side and caller-scoped, whereas this parser reads lifecycle, sector, client and spec ranges in the browser. Decide whether to wire it to the Batch Construction overlay or delete it; do not repurpose it for a governed master search. |
| **Fix in current increment** | SKU Master search — legacy Plant Item Code, Plant Item Code frozen first | Landed 2026-09-16 on Product Owner rulings. `legacy_plant_item_code` joins the identity box's reference kinds (active references only, plant-scoped, capped, reported in `search.fields`); `alias` and `other` stay excluded. Plant Item Code is the FIRST frozen column and the frozen columns now lead the grid. Backend `ee6bf18`, frontend `2e7cbd6`. The box's Item Name / Item Short Name match now reads the latest version only (`2cc8e36`). Gates: route 133/0 then 170/0, `test:sku-master` 102/0 then 115/0. Fixture-browser verified via `u2-skus` only. |
| **Fixed, applied live** | Activate the Amendment 02 SKU Master migration | Applied 2026-09-17, recorded as `20260917024849_u2_sku_master_quote_fields_and_sets` (backend `eb14e7e` renamed the file). The live SKU tables were empty, so nothing was backfilled. There is still no SPEC load (CDM-37/38). Authenticated-live browser proof of the page is not yet recorded. |
| **Fixed, applied live** | SKU pricing portfolio — Canonical Amendment 03 (CDM-45) | Migration `20260917024903_u2_sku_pricing_portfolio` is applied live. Every SKU records exactly `Transactional` or `Strategic`, import must assign it, and it creates no pricing rule. Amendment 04 supplies the governed CAS-protected reclassification route and frontend control for `manage_sku_master`; local evidence is included in the current 195/0 route and 126/0 frontend gates. Authenticated-live mutation proof remains deferred. |
| **Follow-up debt** | SPEC import must obtain a pricing portfolio | The SPEC sheet has no portfolio column, and CDM-45 forbids a default, so the SKU import (CDM-37/38) must obtain one — as a per-import value the operator chooses explicitly, or per row. Unresolved until SPEC import is scoped. |
| **Observation** | Where the portfolio column lives | `public.skus.pricing_portfolio`, not `sku_versions`. This is the one modelling choice Amendment 03 makes that the ruling did not state: the portfolio is not a SPEC column, it classifies the SKU as a commercial object, and C-03 reclassifies it in place. One value per SKU also removes the question of what "this SKU's portfolio" means when two versions disagree. Flagged for Product Owner confirmation; if portfolio history should live on the version chain, the column moves. |
| **Follow-up debt** | U2 SKU Master live qualification | Exercise `/masters/skus` and the Amendment 04 mutations with genuine authenticated callers once governed SKU rows exist, including a Maker without `read_party_master`/`read_construction_library`; obtain Product Owner validation. Open, not ruled (Amendment 02): SET roles for X, S, SL, SLP, SP and PL codes; whether retired partition/plate values may be left behind at import; where glass SKU type is recorded. SKU Sets writes and Location applicability writes remain later Amendment 04 slices. |
| **Fixed, locally verified** | Plant Construction Adoption — read-only | Frontend `2de4c15` turns the pending sidebar entry into a read-only matrix on the governed Construction surface. It shows only approved versions of published Constructions against the caller's exact `plant_access` plants, never widens scope from another capability, and keeps Adopted / Withdrawn / Not adopted / Unavailable distinct. It uses the existing caller-token Construction and Plant reads and exposes no mutation. Gates: focused frontend 10/0, Construction route 38/0, targeted lint and module-contract exit 0; labelled fixture-browser verified only. Governed writes are not a decision-independent follow-on: the accepted surface calls for proposal, approval and withdrawal, while the current table represents only direct `adopted` / `withdrawn` state and has no proposal state or approver attribution. Settle that lifecycle and migrate it before adding controls; do not expose the older direct-write shape as if it were the accepted workflow. See [`u2-plant-construction-adoption-increment.md`](u2-plant-construction-adoption-increment.md). |
| **Fixed locally; activation pending** | SKU Location applicability writes | Product Owner approved Amendment 05 / CDM-47 on 2026-09-17. A prepared, unapplied backend migration adds CAS, immutable binding, governed propose/approve/withdraw/reactivate operations, active same-Customer Location checks, append-only history and direct-write closure for `master` only. Caller-token routes and activation-gated SKU detail controls are local. Gates: schema contract 12/0, route 201/0, frontend 128/0, targeted lint and production build clean. Existing `batch_only` stays readable and has no SKU Master control; an exact Family F/G binding remains a later quotation slice. No remote apply, pgTAP execution or live mutation is included. |
| **Fix in current increment** | Batch commercials seeding regression | Landed in `831e182`: fresh local Batch/new-client drafts retain null commercial override slots so Sector remains authoritative; governed profile values and explicit zero seed exactly. Named fixtures and browser evidence are recorded in [`batch-commercials-seeding-regression.md`](batch-commercials-seeding-regression.md). |
| **Fix in current increment** | Shared construction picker and exact-selection reuse | Landed in `ec13a42`: Batch Builder and Costing retain their distinct link/copy semantics through one active-and-usable picker, and Send reuses the Maker's selected exact duplicate before any first library match. Named fixtures and browser evidence are recorded in [`shared-construction-picker.md`](shared-construction-picker.md). |
| **Fix in current increment** | UX Batch 1 — shared `SummaryRow` | Add the collapsed-by-default disclosure primitive to `src/ui/dataDisplay.jsx`, unused until Batch 4. Keep it presentation-only and suitable for 2–4 facts, a status badge, and an accessible chevron toggle. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §2.1 and §5.1. |
| **Fix in current increment** | UX Batch 2 — shared type scale | Add the disciplined `T` size tokens to `theme.js`, unused until the relevant screen batches. Adopt them incrementally rather than through an app-wide replacement. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §2.2 and §5.2. |
| **Fix in current increment** | UX Batch 3a — row-height constancy | Establish the dense-grid convention that conditional secondary content uses the existing expanded-row mechanism and does not increase compact-row height. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §2.3 and §5.3. |
| **Fix in current increment** | UX Batch 3b — screen-end padding | Establish 40–60px bottom breathing room for scrollable tabs, plus jump links when independent full-height masters share one page. Keep this commit separate from Batch 3a. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §2.4 and §5.3. |
| **Fix in current increment** | UX Batch 4 — Batch Builder pricing disclosure | Landed in `aced13c`: the Pricing `SummaryRow` card is sized like its Customer/Commercials siblings (min 280 / basis 360 / max 420px), and its compact content is two unwrapped rows — BATCH + DATE + icon-sized open controls (full label kept as tooltip and accessible name), then RELEASE + mode badge + an inline status note; an open Batch adds its identity line between them. Browser-measured at 1440px: content 147→51px, profile band 149→99px. `test:pricing-basis` 98/0 with U4-FE-11 updated; `test:pricing-group` keeps its two recorded baseline failures (18a, 19). Batch-open fit was measured by DOM simulation, not a live governed Batch. The same commit carded the Customer Families list entries and Family identity header. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.1. |
| **Fix in current increment** | UX profile bars — 3-row cards and label toggle | Landed 2026-09-15. Batch Builder and Costing card rails run the full card height. Batch Builder has separate Customer / Commercials / Terms / Pricing cards on a 3-row baseline: Terms is one field per row; Pricing is BATCH + open controls · DATE + open-Batch identity + AUTO/Save · RELEASE + mode + note. Import profile and New batch moved to the grid toolbar. Both bar labels open every card when all are closed and collapse all otherwise, reading vertically while any card is open. Browser-measured at 1440px: Batch Builder band 95px, no overflow; Costing label and rails verified live. `test:pricing-basis` 98/0. |
| **Fix in current increment** | Customer Families header and references | Landed 2026-09-15. Sectors and Aliases are tiles inside the Family header; the family list is 280px. External References have their own table column and toggle, because they belong to the Customer/Prospect (`party_external_references.party_id`), not to a Location. Live-verified at 1440px after U4 activation: header card with side-by-side Sectors/Aliases tiles (Sectors keeps its red "Required" state), and separate "N locations" / "N references" toggles in the table. |
| **Fix in current increment** | GSM Master | Landed 2026-09-15. Governed `paper_gsm_values` seeded with the Product Owner list (80–250); migrations `20260915084131_gsm_master` and `20260915084252_gsm_master_catalogue_gates` applied, catalogue gates 6/6, authenticated read and direct-write refusal proven. Read/add/retire/restore routes (writes require `manage_construction_library`; values are never renumbered), GSM Master screen behind `u2_gsm_master`, and GSM dropdowns in Costing and the Construction Library editor that keep retired or off-list stored values visibly. Live-verified: screen read path and Costing dropdown. Add/retire/restore not exercised against live data. |
| **Fix in current increment** | UX policy §3 shell cues and §2.5 visible reasons | Landed 2026-09-15. Shared `ProvenanceTag` (Local / Governed / Immutable, visible word plus detail title) on the Quotes views, My Batches actions, the Customer Families party table and the TopBar Batch context. TopBar shows the active screen, an open governed Batch reference with its version, and local working-row counts on Costing/Batch Builder. Sidebar pending items are styled by reason (Restricted lock/amber, Planned/Future/Disabled dashed and italic, Included neutral, Activation blue). Development fixture previews on the login screen are demoted to small links under a "Developer previews" divider. §2.5 audit: the Pricing card Save reads "Saved" when unchanged and Batch Builder's Send All reads "(calculate first)" while disabled; the other blocked actions already had visible cues (blockers panel, Scratchpad chip, Review strip, readiness list). Live-verified: Quotes tags, TopBar screen names, sidebar Disabled/Included styling, Send All label. Not observable in this session: My Batches action tags (no Batches), the login previews (signed in), the Restricted style and the TopBar Batch chip. `test:quote-evidence` U5-FE-24 updated for the tagged view entry. |
| **Fix in current increment** | UX Batch 4 — Batch Builder compact-row constancy | Move the conditional SET-code confirmation controls and Glass-SKU badge from compact `BatchGrid` rows into the existing expanded-row area without changing SET identity or resolution behavior. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.1. |
| **Fix in current increment** | UX Batch 4 — Batch Builder toolbar density | Move Auto-code, Generate Missing Codes, and the format hint into a small secondary popover while leaving primary grid actions immediately visible. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.1. |
| **Fix in current increment** | UX Batch 4 — Batch Builder focus mode | Add a toolbar toggle that reuses the existing sidebar-collapse mechanism and presents `BatchProfileBar` as a one-line summary; do not use the browser Fullscreen API. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.1. |
| **Fix in current increment** | UX Batch 5 — Costing KLD placement | Move the read-only die-line preview from `SpecForm` to the top region of `OutputPanel` without changing its inputs or data flow. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.2. |
| **Fix in current increment** | UX Batch 5 — Costing slider density | Place Margin and Fluting BS Contribution controls side by side at half width and keep margin presets usable without wrapping. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.2. |
| **Observation** | UX Batch 5 — Costing layer-detail placement | Settled by Product Owner, 2026-09-15: Layer Detail stays where it is (beside the BS control, after the Die-Line / Cost Build-up row). Make no further layout changes to Costing `OutputPanel` under the UX policy. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.2. |
| **Fix in current increment** | UX Batch 5 — Costing incomplete-state density | Once some inputs are filled, show remaining required fields as a tight single-line list rather than repeated illustrated cards. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.2. |
| **Fix in current increment** | UX Batch 6 — Rate Master price-rules disclosure | Collapse Price Rules into `SummaryRow` by default so the rate table leads, preserving `buildBlanketConfirm` exactly. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.3. |
| **Fix in current increment** | UX Batch 6 — Rate Master bottom spacing | Add the shared screen-end breathing room after the table. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.3. |
| **Fix in current increment** | UX Batch 6 — Commercial Policies navigation | Add jump links for Sector Defaults, Box Trim Defaults, and Partitions Master. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.4. |
| **Fix in current increment** | UX Batch 6 — Commercial Policies spacing and headings | Add shared screen-end breathing room and normalize section headings to the shared type scale. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.4. |
| **Fix in current increment** | UX Batch 7 — Customer Families party table | Present current Customers/Prospects as a Code / Name / Status / Locations / Actions table with locations expanded on demand. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.5. |
| **Fix in current increment** | UX Batch 7 — Customer Families section disclosures | Use `SummaryRow` for Sectors, Aliases, Membership History, and each Party's Locations and External References, collapsed by default. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.5. |
| **Fix in current increment** | UX Batch 7 — Customer Families information architecture | Complete the table/card presentation so the screen follows the application's established master-screen idiom. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.5. |
| **Fix in current increment** | UX Batch 7 — Pricing Basis component disclosures | Use `SummaryRow` for each Rate, Freight, and Sector `BasisPart`, collapsed by default. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.6. |
| **Fix in current increment** | UX Batch 7 — Pricing Basis rate drilldown | Make `RateDrilldown` the second expansion level beneath the Rate basis summary. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.6. |
| **Follow-up debt** | Governed Sector master is empty | U4 Family–Sector classification was activated on 2026-09-15 as `20260915100440_u4_customer_family_sectors` and `20260915100521_u4_customer_family_sector_catalogue_gates` (renamed from their authored `20260912183000`/`184000` names to the recorded remote versions; statement MD5 matches; `tests.u4_customer_family_sector_catalogue()` 7/7). The gates file was corrected before activation (its `return next (expr, label)` form is invalid in an OUT-parameter function). There were 0 Batches, so no memberships were back-filled. `public.sectors` currently holds no rows, so proposing a Family, creating a Prospect, approving a Family, adding a Family Sector and creating a governed Batch all refuse with "an active Sector is required" until governed Sectors exist. How Sector Defaults connect to governed Sectors is the separately parked Sector data-model decision. |
| **Observation** | Cust Type stays manual | Product Owner ruling, 2026-09-15: Cust Type is not derived from party lifecycle for now. It feeds only the optional Costing margin suggestion (`OutputPanel.jsx` "✦ Use X%") and the Customer card badge. If revisited, any mapping (prospect → New, customer → Existing) should fill only a still-default value on a single exact client match and never overwrite a manual Strategic/Spot choice. |
| **Observation** | UX Batch 8 — SKU Master presentation criteria | Applied from the start in the read-only SKU Master (2026-09-15): `PermanentCode` + `LifecycleBadge` + `ProvenanceTag` identity header; `SummaryRow` for Versions, Specifications, External references and Location applicability; `T` sizes throughout. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.7. |
| **Fix in current increment** | Screen-space standard — shared module | Landed 2026-09-16. The SKU Master's toolbar height, control and disclosure tokens, dense-row and frozen-identity styles and the panel focus / split mechanics moved into `src/lib/panelSplit.js`, `src/ui/screenStandards.js` and `src/ui/screenChrome.jsx` (commit `7f07188`), so "one toolbar per panel at a shared height" is one number rather than a coincidence between four screens. `skuMasterModel.js` re-exports the split arithmetic under its existing names, so every SKU Master import and named gate is unchanged. Tokens and hooks live in the `.js` module and components in the `.jsx` one because mixing them breaks Fast Refresh, which this repository's lint config enforces. Behaviour unchanged; `test:sku-master` 87/0. |
| **Fix in current increment** | UX — My Batches, Approval Inbox and Quotes screen space | Landed 2026-09-16: `fee27cd` (My Batches), `eaae91d` (Approval Inbox, Quote History, Quotes). Audit first, then the rewrite. Browser-measured at 1440×900 with the fixture banner hidden, plus the 48px TopBar — first data row / compact row height, before → after: **My Batches** 328px / 133px → 130px / 27px; **Approval Inbox** 274px / 72px → 111px / 26px; **Quote History** ~333px plus a 49px view-switch band / 73px → 111px with no band / 27px. Benchmark SKU Master: 91px / 26px. Applied: no page header (the TopBar names the screen, and the dead `.batch-catalogue-*`, `.quote-catalogue-*`, `.quotes-screen-header`, `.quote-disabled-actions` and `.quotes-view-switch` rules left `index.css` with the markup); one toolbar per panel at the shared 43px, with the Quotes view switch moved INTO the active view's toolbar; secondary controls in `<details>` disclosures; dense rows with a frozen identity column and every secondary fact in a row disclosure, so a compact row is never made taller by detail; Quote evidence promoted from a section below the table to its own panel with a draggable divider and shared expand/collapse icons (inside the app window — never the Fullscreen API); provenance and legend once in a footer; `T` tokens throughout. **S9 unchanged:** every activation-blocked action stays visible and disabled carrying `Backend activation pending`, folded into the one toolbar rather than hidden behind a disclosure or given a band. Gates: `test:batch-catalogue` 23/0 (was 14/0), `test:quote-evidence` 49/0 (was 38/0), `test:sku-master` 87/0, every other named fixture gate re-run green, eslint clean on changed files, module-contract build exit 0. Fixture-browser verified via the `u4-batches`, `u5`, `u5-inbox` and `u5-history` developer previews only — **not** authenticated-live. |
| **Follow-up debt** | Two Quotes labels shortened to fit one toolbar row | The view switch reads Working / Governed / History with the full view name in `name` and on hover, and the workflow filter's first option reads "All states" rather than "All workflow states". Both were needed to keep the Quote History toolbar on one row at the default 50 : 50 split on a 1440px viewport (measured: 692px of controls in 689px of space before the trim). Product Owner may reinstate either wording; the cost is a second toolbar row on that view. |
| **Fix in current increment** | SKU Master column-header filters | Landed 2026-09-16 on Product Owner rulings (latest version; no migration; text contains / numbers between / closed vocabularies a value list, each with blank and not blank; screen state). Backend `2cc8e36`: `GET /masters/skus?f=field:op:value`, 39 filterable API fields (the commit message's "42" is a miscount), every sub-query caller-token and `plant_id in <plant_access plants>`, capped with per-filter `scan_truncated`, version fields matched on the latest version, gated masters refused `CAPABILITY_REQUIRED` and pending storage refused `SCHEMA_ACTIVATION_PENDING` before any read. Frontend `ed4f249`: `src/lib/skuColumnFilters.js` classifies all 40 registry columns (39 filterable, Flute Type derived), a funnel in every header disabled with its reason when it cannot filter, lifecycle and portfolio sharing one state with their toolbar controls, "Filter reach" reporting. The identity box is unchanged. Gates: route 170/0, `test:sku-master` 115/0, every backend test script green. Fixture-browser verified only — **not** authenticated-live; live filtering of Amendment 02/03 fields needs those migrations applied. |
| **Fix in current increment** | UX — Quote Items, Commercial Masters, Users & Access, Producing Plants, GSM Master, Customer Families, Pricing Basis drill-downs | Landed 2026-09-16 on the shared screen-space standard, audit first. `c5e4729` Quote Items (first row y 310 → 113, rows 39/63 → 26px, SET header+total → one row, false re-import advice removed); `b2749d2` Commercial Policies / Rate / Freight (226/240/236 → 113, rows 35–36 → 26; one section at a time; `buildBlanketConfirm` verbatim; confirms added to Reset to Defaults, Partitions delete, Freight location delete); `8965a0e` Users & Access (337 / 42–43px → 113 / 26px) and Producing Plants (177 / 69–70px → 113 / 26–27px), new gate `test:screen-standard`; `eda0875` GSM Master; `77be0c0` Pricing Basis drill-down tables; `a85e325` Customer Families (list cards → dense table with all 14 fixture Families visible, Customers/Prospects 44px → 26–27px from y 496 → 170, no horizontal scroll, Locations opened directly). Measured in the app shell with a fixture profile over a stubbed transport — fixture-browser evidence only. Gates: `test:screen-standard` 30/0, `test:quote-evidence` 55/0, all 23 named frontend gates green, module-contract build exit 0. |
| **Follow-up debt** | Commercial Masters edit gate reads the role label | `DefaultsTab`, `RateMasterTab` and `FreightTab` unlock editing on `role==="admin"`, the derived label, against the U1 rule that screens gate on capabilities. Kept unchanged by Product Owner ruling (2026-09-16) because these are browser-local legacy masters with no governed capability; only the false "Switch to Admin" wording was corrected. Re-gate when these masters become governed. |
| **Fix in current increment** | SKU Master editing by due authority — slice 1 (Amendment 04, CDM-46) | Ruled 2026-09-16 ([`data-model-canonical-amendment-04.md`](data-model-canonical-amendment-04.md)): same-person approval allowed initially as a speed-first canonical principle; field classes, code/publish split, lifecycle with reason and same-plant same-Customer replacement, withdrawal, add/withdraw references, compare-and-swap, append-only history, direct write path closed, controls visible-and-disabled until activation. Backend `863e652` migration `20260916200000_u2_sku_master_governed_operations` (applied live 2026-09-17 as `20260917030403`; static contract 85/0; live pgTAP `tests.sku_governed_operations` 67/0, registered in `run_all`), `c515108` twelve governed routes, `4e4cbde` activation signal; route gate 195/0. Frontend `603af76` Actions / version editor / references / Propose / History; `test:sku-master` 126/0. Fixture-browser verified only; no governed write has been exercised. |
| **Fix in current increment** | What makes a customer "settled", and how quotation use is gated | Ruled 2026-09-16 (Amendment 04 D-13, D-14): settled = a Party graduated to Customer with a permanent Customer Code; a Proposed SKU and an unapproved version are calculated, sent and offered in the Batch picker exactly as a Prospect is admitted incomplete, and only a withdrawn SKU is refused. Backend `0ba4b1b` (migration `20260916210000_u2_proposed_skus_are_quotable`, contract 10/0, route gate 101/0), frontend `e15376c` (picker labels unapproved versions). Applied live 2026-09-17 as `20260917030435`; the live `assert_calculate_eligible` and `send_batch` carry `sku_withdrawn` and neither old refusal. |
| **Follow-up debt** | Verification owed after SKU migration activation | All five applied live 2026-09-17: 02 `20260917024849`, 03 `20260917024903`, fixtures `20260917025921`, 04 `20260917030403`, quotability `20260917030435`. Live pgTAP (each suite alone, rolled back): `sku_governed_operations` 67/0, `product_workflow` 68/0, `sku_master` 47/0, `family_c_authority` 23/0. Still owed: the Batch-creating suites (`batch_sets`, `batch_workspace`, `family_f_security`, `calculation_writer`, `calculation_persistence`) abort on the pre-existing empty Customer Family Sector master, and `suite_registration` fails SR-1 on the unregistered `gsm_master_catalogue` / `u4_customer_family_sector_catalogue` suites (not this work). No authenticated-live browser proof of SKU editing or Proposed-SKU quoting. |
| **Fixed, locally verified** | SKU Master shared split and focus controls | `SkuMasterScreen.jsx` now uses `usePanelFocus`, `useSplitPanels`, `PanelFocusToggle` and `PanelDivider`; its copied focus effect, pointer-drag handler and divider markup are gone. The focused gate was repointed to the shared implementation rather than weakened: SKU Master 126/0, shared screen standard 30/0, Construction Adoption 10/0, targeted lint and module-contract exit 0. Labelled fixture-browser verification exercised list focus, detail focus, Escape restoration, the 65:35 preset and double-click reset to 50:50. No governed read, write, capability or activation boundary changed. |
| **Observation** | `npm run lint` is not clean at baseline | `eslint .` reports errors across `src/engine`, `src/export`, `src/state`, `src/components` and `AuthContext.jsx`, plus the whole locked agent worktree under `.claude/worktrees/`, which `eslint .` descends into. This predates the 2026-09-16 UX work. Lint changed files explicitly; do not read a repo-wide count as a regression signal, and do not bulk-fix (§ Editing rules forbids `eslint --fix` across the repository). |
| **Follow-up debt** | UX Batch 8 — Plant Configuration presentation criteria | When Plant Configuration exists, build a Plant header with `SummaryRow` disclosures for Machines, Stations, and Process Routes rather than a linear indented hierarchy. `ProducingPlantsScreen` needs no interim change. Source: [`ux-design-review-policy.md`](ux-design-review-policy.md) §4.8. |
| **Follow-up debt** | U3 Pricing Basis live qualification | Local implementation and the focused automated gate are complete (`test:pricing-basis` 98/0, route 31/0 on 2026-09-15). Local fixture-browser evidence (2026-09-15, `?fixture=u3-u4`): release eligibility, lifecycle chronology, plant-enforced composition, Rate version history (v5 Draft / v4 Approved / v3 Withdrawn) and Sector-vs-Default inheritance with `0.000% · explicit zero` distinct from fallback values. Genuine authenticated-live walkthrough and Product Owner validation remain deferred (Product Owner unavailable). |
| **Follow-up debt** | Job-work pricing and client-owned Kraft | Post-beta canonical gap ruled 2026-09-18. Limited beta excludes every job-work enquiry; Makers keep them on the spreadsheet and may not approximate them with a non-`J` grade or zero material rate. After beta evidence exists, decide conversion-only versus material-at-zero versus a separate rate basis, whether client-owned paper needs stock tracking, and whether the `J` suffix belongs on the grade or on the Batch row. Do not design it during beta. |
| **Follow-up debt** | U4 My Batches live qualification | Authenticated-live browser verification and Product Owner validation are deferred. The U4 Family–Sector migrations are activated as `20260915100440` and `20260915100521` (see "Governed Sector master is empty"). The focused U4 frontend aggregate passes **56/56** (catalogue 14, lock 5, row lifecycle 6, Pricing Group 31) since `970ee95` corrected the two stale assertions to the current 37-column shared span and flexible Pricing card, with no UI change. Local fixture-browser evidence (2026-09-15, `?fixture=u4-batches`): bounded catalogue, partial-detail disclosure naming the denied field, Governed/Immutable tags, and Submit/Approve/Return/Issue disabled. Add server-side pagination/search/filtering when either trigger in [`u4-my-batches-catalogue-increment.md`](u4-my-batches-catalogue-increment.md) occurs. |
| **Follow-up debt** | U5 read-only workflow live qualification | Approval Inbox, Quote History, immutable evidence/customer outcomes, exact identity presentation, and guarded Batch navigation are locally implemented and automated-test verified (`test:quote-evidence` 38/0, `test:governed-calculate-send` 9/0, route 27/0 on 2026-09-15). Local fixture-browser evidence (2026-09-15, `?fixture=u5`, `u5-inbox`, `u5-history`): revision chronology, frozen Item/Snapshot/Pricing Group/Freight Set Version/Freight Entry identities, customer outcomes, and the disabled Submit, Approve, Return, Withdraw, Issue, Create revision, Amend and Reprice actions (with Calculate, Send, Open Quote, Open current Batch). Live callers additionally need the deployment-dependent Family G read correction. Product Owner validation remains deferred. |
| **Follow-up debt** | Party merge behavior | The dependent-handling product decision remains unresolved. Keep the operation dormant until the Product Owner rules on the choices in [`u1-party-merge-decision-packet.md`](u1-party-merge-decision-packet.md). If option (a) is chosen, SKUs stay on the merged Party; the SKU Master shows that owner as recorded and does not reassign. |
| **Follow-up debt** | U1 decision- and S10-dependent lifecycle operations | Re-audited 2026-09-15: no decision-independent technical gap. Users/Access, Customer/Prospect/Location creation, the limited master-backed interaction, Party external-reference reads and Producing Plant relevant assignments are delivered. Party deactivation/reactivation and post-proposal Location eligibility change await Product Owner decisions; Location-to-Party reassignment has no schema mechanism; Party lifecycle history and audit presentation depend on S10/U6. No U1 code change was made. |
| **Follow-up debt** | Deferred post-model defects | Promote a specific item from [`post-model-defects.md`](post-model-defects.md) only when it is deliberately scoped or blocks an accepted increment. Do not reopen the historical defect pass wholesale. |
| **Follow-up debt** | Feature requests | Items in [`feature-requests.md`](feature-requests.md) are recorded, not specified or authorised. Promote only a selected request with an acceptance outcome. |
| **Follow-up debt** | Printing specification backend completion | The local frontend now carries `printing_technology` and `number_of_colours` between Costing and Batch Entry as row-owned, non-calculation-driving metadata. The vocabulary is ruled (Flexo / CMYK / Offset / Unprinted, Amendment 02 B-06) and SKU version storage is prepared in the unapplied Amendment 02 migration. Still to do: persist them on governed Batch rows, then carry them through durable Send snapshots, exports, and SKU Master search. Source: [`data-model-decisions.md`](data-model-decisions.md) CDM-10 and [`data-model-frontend-design-plan.md`](data-model-frontend-design-plan.md) §5.3. |
| **Follow-up debt** | Backend build identity | The running Flask service still lacks a reliable build/version signal. Add one when backend deployment verification is in scope; until then, restart deliberately and record what revision was exercised. |
| **Observation** | Transitional persistence | Legacy `cbb_*` browser persistence and governed Supabase-backed surfaces coexist. A backup of browser keys is not a complete backup of governed database records. |
| **Observation** | Dirty worktrees | Preserve ownership and inspect both repositories before drawing project-wide conclusions. At 2026-09-15 the preserved, unowned items were: the locked agent worktree `quote-gen-fe/.claude/worktrees/agent-a2b53170ad09a5edf` (branch at `59b9989`, five uncommitted edits to `pricing-basis-fixtures.mjs`, `index.css`, `CustomerFamiliesScreen.jsx`, `BatchPricingBasisSelector.jsx`, `BatchPricingBasisWorkspace.jsx`); and the S9 qualification thread's `docs/README.md` index line plus untracked `docs/s9-qualification-seed-and-quote-read-fix-plan.md`. |

There is no remaining **Fix in current increment** item for the documentation cleanup once its link,
section, and Git-scope checks pass. Unrelated defects stay non-blocking.
