# Backend URL used for production builds (`npm run build`).
# Committed deliberately: Vite inlines this into the client bundle at build
# time, so it is public by nature — it is configuration, not a secret.
# A VITE_API_BASE set in the Vercel dashboard overrides this file.
VITE_API_BASE=https://quote-gen-be.vercel.app
